DATABASE_URL=postgres://space:change-me@localhost:5432/space
# Local PostgreSQL only. Keep `require` for remote/production databases.
DATABASE_SSL_MODE=disable
SPACE_PORT=8787
SPACE_PUBLIC_ORIGIN=http://localhost:8787
SPACE_TOKEN_PEPPER=replace-with-at-least-32-random-bytes
SPACE_LOG_LEVEL=info
