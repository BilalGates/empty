{
  "name": "space-password-manager",
  "version": "0.1.0",
  "private": true,
  "description": "A zero-knowledge personal password manager",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "vitest run --exclude apps/extension/test/** && npm test --workspace @space/extension",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint .",
    "typecheck": "tsc -b",
    "check": "npm run lint && npm run typecheck && npm test && npm run build",
    "release:extension": "npm run package --workspace @space/extension",
    "security:secrets": "node scripts/secret-scan.mjs",
    "security:permissions": "node scripts/check-extension-permissions.mjs"
  },
  "devDependencies": {
    "@eslint/js": "9.35.0",
    "@types/node": "22.18.1",
    "@vitest/coverage-v8": "^3.2.7",
    "eslint": "9.35.0",
    "globals": "16.3.0",
    "typescript": "5.9.2",
    "typescript-eslint": "8.42.0",
    "vitest": "^3.2.7"
  }
}
