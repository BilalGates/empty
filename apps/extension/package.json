{
  "name": "@space/extension",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test test/*.test.js",
    "check": "npm run test && npm run build"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
