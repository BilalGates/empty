NODE_ENV=development
HOST=127.0.0.1
PORT=3000
DATABASE_URL=postgres://space:replace-me@127.0.0.1:5432/space
# Local PostgreSQL only. Keep `require` for remote/production databases.
DATABASE_SSL_MODE=disable
DEVICE_TOKEN_PEPPER=replace-with-at-least-32-random-characters
LOG_LEVEL=info
